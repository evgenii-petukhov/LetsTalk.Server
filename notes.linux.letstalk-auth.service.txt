[Unit]
Description=LetsTalk

[Service]
WorkingDirectory=/repos/LetsTalk.Server/
ExecStart=dotnet /repos/LetsTalk.Server/LetsTalk.Server.Authentication/bin/Debug/net7.0/publish/LetsTalk.Server.Authentication.dll
Restart=always
# Restart service after 10 seconds if the dotnet service crashes:
RestartSec=10
KillSignal=SIGINT
SyslogIdentifier=letstalk
#Environment=ASPNETCORE_ENVIRONMENT=Production

[Install]
WantedBy=multi-user.target

sudo systemctl daemon-reload
